# Lattice Interiors for 3D Gaussian Splatting

Project page. The site is served from the repository root; `index.html` and `assets/` are the
whole of it.

Numbers on the page are measured under one protocol on one machine and include the results that
did not work.
